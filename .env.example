# Optional: server port (defaults to 3000)
PORT=3000

# Optional legacy fallback: acts as the Admin PIN until you set one in
# Settings > Access & Roles inside the app (which is the recommended way
# to manage Staff/Admin PINs — no redeploy needed).
ACCESS_PIN=
